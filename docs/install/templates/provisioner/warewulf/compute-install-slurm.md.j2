<!-- ohpc_begin -->
<!-- ohpc_comment Add SLURM and other components to compute instance -->
```bash
wwctl image exec --build=false {{ baseos }} -- /bin/bash -ex <<- EOF
  # Add Slurm client support meta-package
  {{ "apt -y install" if is_ubuntu else "dnf -y install" }} ohpc-slurm-client

  # Enable services to start on boot
  systemctl enable munge.service
  systemctl enable slurmd.service

  # Add Network Time Protocol (NTP) support
  {{ "apt -y install" if is_ubuntu else "dnf -y install" }} chrony

  # Include modules user environment
  {{ "apt -y install" if is_ubuntu else "dnf -y install" }} lmod-ohpc
EOF
```
<!-- ohpc_end -->

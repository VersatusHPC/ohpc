Ubuntu 24.04 does not currently provide the Omni-Path package set used by the
upstream EL and openEuler recipes. Sites that require Omni-Path should add
vendor-supported Ubuntu packages or a local repository before enabling this
option.

If your compute resources support Omni-Path, install the vendor-supported Ubuntu
packages in the compute image before enabling this option.

% begin_ohpc_run
% ohpc_validation_comment Add Omni-Path drivers to compute image
% ohpc_command if [[ ${enable_opa} -eq 1 ]];then
% ohpc_indent 5
\begin{lstlisting}[language=bash,literate={-}{-}1,keywords={},upquote=true]
# Add site-provided OPA support packages here.
[sms](*\#*) echo "Install site-provided Omni-Path packages in $CHROOT."
\end{lstlisting}
% ohpc_indent 0
% ohpc_command fi
% end_ohpc_run
